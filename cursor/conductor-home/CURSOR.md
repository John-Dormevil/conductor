# Conductor Context (Cursor)

If the user mentions a "plan" or asks about the plan after using Conductor in this session, they likely mean `conductor/tracks.md` or `conductor/tracks/<track_id>/plan.md`.

**Global install path:** `~/.cursor/conductor/`  
**Commands:** `/conductor-setup`, `/conductor-new-track`, `/conductor-implement`, `/conductor-status`, `/conductor-revert`, `/conductor-review`

See `~/.cursor/skills/conductor/SKILL.md` for the Universal File Resolution Protocol.
